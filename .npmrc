# Force devDependencies to always install, even when NODE_ENV=production is
# set in the shell (this sandbox exports NODE_ENV=production by default,
# which otherwise makes npm silently omit @types/*, tailwindcss, eslint, etc).
include=dev
